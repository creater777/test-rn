import {API_USER, API_KEY} from '@env';

export const UPDATE_TIME = 15;
export const REFRESH_TIMEOUT = 60;
export const PAGE_SIZE = 25;
export const BASE_URL = `https://${API_USER}:${API_KEY}@api.github.com`;
